import 'package:flutter/material.dart';

class VistaBienvenidos extends StatelessWidget {
  const VistaBienvenidos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
